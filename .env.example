# Cloudflare named-tunnel connector token.
# Zero Trust dashboard > Networks > Tunnels > (your tunnel) > Install connector.
# In that tunnel's Public Hostname config, set the service to: http://web:80
# Copy this file to .env (gitignored) and fill in the token.
TUNNEL_TOKEN=
